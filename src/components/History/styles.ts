import { StyleSheet } from "react-native";

export default StyleSheet.create({
  historyContainer: {
    marginBottom: 10,
  },
  historyTitle: {
    fontSize: 16,
    fontWeight: "bold",
    marginBottom: 5,
  },
  historyItem: {
    padding: 10,
    borderBottomWidth: 1,
    borderBottomColor: "#ddd",
  },
});
